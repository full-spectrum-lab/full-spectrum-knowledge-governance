# K2 team02 架构设计与研发实现

状态：`ACCEPTED / OFFLINE SLICE / LATEST 8dbaae9`

## 模块

- Contracts：来源注册、检索信封、动态快照、审计事件；
- Domain：身份、生命周期、摘要和绑定规则；
- Storage：SQLite 追加式记录、不可变快照、审计链和回放；
- TestHost：`verify-k2` 离线验证命令。

审计事件使用前序摘要和事件摘要形成链式完整性校验。快照必须引用已存在的检索记录，并匹配来源、版本、适配器、清洗摘要和规范化摘要。快照摘要由规范化字段确定性计算。

代码不执行网络访问，不写入固定知识表，不改变 `FIXED_ONLY` 解析器。
