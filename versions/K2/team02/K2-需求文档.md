# K2 team02 需求文档：离线受控知识来源与快照切片

状态：`ACCEPTED / FINAL IMPLEMENTED SLICE / INTERNAL ONLY / NOT RELEASED`

本迭代承接历史 `versions/K2/team/` 设计，仅实现离线治理切片：来源注册、生命周期、检索信封、不可变快照、审计回放与绑定校验。

不包含网络抓取、真实来源、Dynamic/Hybrid 解析、自动晋级固定知识、Observer/Engine 集成或生产授权。

验收必须证明：来源身份精确、撤销后禁止新检索、快照绑定已记录检索、摘要可复算、父快照关系受保护、审计可回放且篡改失败，原有 FIXED_ONLY 行为不变。实现证据为 `103/103 PASS` 与 `verify-k2 = PASS`。
