# K2 team02 测试方案

状态：`ACCEPTED / VERIFIED LOCALLY`

测试范围：生命周期正向/负向、重启恢复、重复注册、检索冲突重试、快照不可变、检索绑定、父快照关系、摘要篡改、Schema 严格字段、审计回放和固定知识回归。

执行入口：`FullSpectrum.Knowledge.TestHost verify-k2`；全量回归测试必须全部通过。Golden 与负面案例只使用合成材料，不接真实网络或个人数据。

预期边界：网络访问 `NOT_EXECUTED_BY_DESIGN`，Dynamic/Hybrid `DEFERRED`，生产就绪 `NO`。
