# K2 team02 WorkBuddy 验收文档

状态：`ACCEPTED / VERIFIED LOCALLY / NOT RELEASED`

1. 使用最新提交 `8dbaae9` 对应源码，确认工作树和 SDK 环境。
2. 执行 `dotnet restore FullSpectrum.Knowledge.slnx --locked-mode`、Release 构建和全量测试。
3. 执行 `dotnet run --project src/FullSpectrum.Knowledge.TestHost -c Release --no-build -- verify-k2`。
4. 核对输出中 lifecycle、retrieval_snapshot_binding、audit_replay 均为 `PASS`。
5. 核对全量回归为 `103/103 PASS` 或更新后的实际总数全通过。
6. 记录 Schema、Golden、审计链篡改、快照摘要篡改和父快照缺失的负面结果。

验收证据：`acceptance-logs/final-20260902-105500/`；F1 复验：`acceptance-logs/f1-lockfile-reverify-20260902-120400.md`。`8dbaae9` 已修复锁文件卫生问题，`--locked-mode` 复验通过，team02 本机验收结论为 `PASS`。

不得将本验收写成完整 K2、网络来源可用、Dynamic/Hybrid 已实现或生产就绪。独立主机验证应单独标记，不得由本机结果替代。
