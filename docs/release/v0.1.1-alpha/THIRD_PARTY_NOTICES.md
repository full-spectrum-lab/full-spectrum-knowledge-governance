# Third-party notices

License expression: `MulanPSL-2.0 OR Apache-2.0`. Complete texts are included as `LICENSE-MULANPSL-2.0` and `LICENSE-APACHE-2.0`.

No external NuGet PackageReference is declared. The framework-dependent .NET runtime and operating-system SQLite library are prerequisites, not bundled payloads. The generated `SBOM.package.spdx.json` records every packaged file and SHA-256.

Only synthetic fixtures are included.
