{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://full-spectrum-lab.example/schemas/knowledge/v1.0/knowledge-resolution-request.schema.json",
  "title": "Knowledge Resolution Request",
  "type": "object",
  "required": ["contract_version", "request_id", "mode", "subject_digest", "required_slots", "context"],
  "properties": {
    "contract_version": { "const": "knowledge-contract/1.0.0" },
    "request_id": { "type": "string", "minLength": 1 },
    "mode": { "enum": ["FIXED_ONLY", "DYNAMIC_ONLY", "HYBRID"] },
    "subject_digest": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "required_slots": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 }
    },
    "context": {
      "type": "object",
      "additionalProperties": { "type": "string" }
    }
  },
  "additionalProperties": false
}
